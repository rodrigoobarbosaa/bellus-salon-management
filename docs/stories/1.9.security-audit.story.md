---
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["build_check", "security_review"]
---

# Story 1.9: Security Audit — Revisão & Hardening Final

**Status:** Done
**Epic:** EPIC-1 Bellus — Wave 1 Fundação
**Criado por:** River (@sm)
**Data:** 2026-03-22
**Depende de:** Story 1.3 (autenticação), Story 1.6 (equipe)

---

## Story

**As a** proprietário do salão,
**I want** que o aplicativo seja seguro contra vulnerabilidades comuns,
**so that** os dados dos meus clientes e do negócio estejam protegidos.

---

## Acceptance Criteria

1. Todas as server actions validam autenticação (user != null) antes de qualquer operação
2. Todas as queries incluem filtro `salao_id` (isolamento multi-tenant)
3. Nenhuma API route pública expõe dados sem autenticação (exceto `/api/booking/slots`, `/api/opt-out`, `/booking/[slug]`)
4. Inputs de formulário validados: email format, string length limits, numeric ranges
5. Nenhum segredo (.env keys) hardcoded no código fonte
6. Headers de segurança configurados: X-Frame-Options, X-Content-Type-Options, Referrer-Policy
7. Rate limiting básico em endpoints públicos (`/api/booking/slots`)
8. Server actions de equipe.ts validam enum `role` antes de cast
9. `updateProfissionalServicos` verifica que serviços pertencem ao mesmo salão
10. `npm run build` sem erros

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> Validação de qualidade usará processo de revisão manual.

---

## Tasks / Subtasks

- [x] Task 1: Audit server actions — auth check (AC: 1, 2)
  - [x] Verificar todas as server actions em `app/actions/*.ts` (14 files audited)
  - [x] Confirmar que cada action chama `getSalaoId()` ou equivalente
  - [x] Confirmar que queries usam `.eq("salao_id", salaoId)`
  - [x] Gaps encontrados e corrigidos:
    - `servicos.ts` `updateServico` — adicionado `.eq("salao_id", salaoId)`
    - `agendamentos.ts` `cancelAgendamento` — adicionado salao_id na query de filhos
    - `agendamentos.ts` `rescheduleAgendamento` — adicionado salao_id no fetch e update
    - `public-booking.ts` — adicionado salao_id na validação de serviço e profissional

- [x] Task 2: Audit API routes — auth check (AC: 3)
  - [x] Verificar todas as routes em `app/api/**/route.ts` (10 routes audited)
  - [x] Routes protegidas requerem auth (marketing/chat, marketing/generate, push/register)
  - [x] Cron routes protegidas com CRON_SECRET (5 routes)
  - [x] Routes públicas intencionais: /api/booking/slots, /api/opt-out (HMAC-protected)

- [x] Task 3: Input validation hardening (AC: 4, 8)
  - [x] Adicionar validação de email format em `equipe.ts` (createProfissional, updateProfissional)
  - [x] Adicionar validação de enum `role` em `equipe.ts` (aceitar apenas "proprietario" | "profissional")
  - [x] Adicionar validação de `preco_override >= 0` em `updateProfissionalServicos` e `updateServicoProfissionais`
  - [x] Adicionar string length limits: nome (100), telefone (20)
  - [x] Verificar validações existentes em outros actions (servicos.ts já valida preco_base, duracao)

- [x] Task 4: Cross-tenant isolation (AC: 9)
  - [x] Em `updateProfissionalServicos`: verificar que cada `servico_id` pertence ao salão via `.in("id", ids).eq("salao_id", salaoId)`
  - [x] Em `updateServicoProfissionais` (servicos.ts): verificar que cada `profissional_id` pertence ao salão

- [x] Task 5: Secrets & headers audit (AC: 5, 6)
  - [x] Grep por API keys/tokens — nenhum segredo hardcoded encontrado
  - [x] `.env*.local` confirmado no `.gitignore`
  - [x] Headers de segurança configurados no `next.config.ts`: X-Frame-Options (DENY), X-Content-Type-Options (nosniff), Referrer-Policy, Permissions-Policy, HSTS

- [x] Task 6: Rate limiting (AC: 7)
  - [x] Rate limiting in-memory adicionado ao `/api/booking/slots`
  - [x] 30 requests por minuto por IP, com cleanup automático a cada 5 min

- [x] Task 7: Validação e build (AC: 10)
  - [x] `npm run build` — sem erros
  - [x] `npm test` — 31 testes passando (3 novos testes de validação)

---

## Dev Notes

### Padrão de Validação de Input

```typescript
// Validação de email
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
if (!emailRegex.test(email)) {
  return { error: "Email inválido." };
}

// Validação de enum
const validRoles = ["proprietario", "profissional"] as const;
if (!validRoles.includes(role as typeof validRoles[number])) {
  return { error: "Cargo inválido." };
}

// Validação de comprimento
if (nome.length > 100) {
  return { error: "Nome muito longo (máximo 100 caracteres)." };
}
```

### Rate Limiting In-Memory

```typescript
const rateLimit = new Map<string, { count: number; resetAt: number }>();

function checkRateLimit(ip: string, limit = 30, windowMs = 60000): boolean {
  const now = Date.now();
  const entry = rateLimit.get(ip);
  if (!entry || now > entry.resetAt) {
    rateLimit.set(ip, { count: 1, resetAt: now + windowMs });
    return true;
  }
  entry.count++;
  return entry.count <= limit;
}
```

### Referências

- **Server actions**: `bellus/src/app/actions/*.ts`
- **API routes**: `bellus/src/app/api/**/route.ts`
- **Config**: `bellus/next.config.ts`
- **Middleware**: `bellus/src/middleware.ts`

---

## Testing

- Tentar acessar server actions sem auth — deve retornar erro
- Tentar inserir role inválido — deve rejeitar
- Tentar email malformado — deve rejeitar
- Verificar que `/api/booking/slots` funciona sem auth (público)
- Rate limit: enviar 31+ requests rápidos — deve bloquear
- `npm run build` — sem erros

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-03-22 | 1.0 | Story criada — security audit e hardening | @sm (River) |
| 2026-03-22 | 1.1 | Validação PO: GO (9.5/10) — Status Draft → Ready | @po (Pax) |
| 2026-03-22 | 1.2 | Security hardening completo — 7 gaps corrigidos, rate limiting, input validation | @dev (Dex) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (@dev Dex)

### Debug Log References
- Nenhum erro de build ou teste durante implementação

### Completion Notes
- Auditoria completa de 14 server action files e 10 API routes
- 7 vulnerabilidades corrigidas: 4 CRITICAL (missing salao_id), 2 HIGH (cross-tenant), 1 MEDIUM (missing validation)
- Input validation adicionada: email regex, role enum, nome length (100), telefone length (20), preco_override >= 0
- Cross-tenant isolation: servicos validados contra salão em updateProfissionalServicos, profissionais validados em updateServicoProfissionais
- Rate limiting: 30 req/min por IP no /api/booking/slots com cleanup automático
- Public booking: serviço e profissional agora validados contra salao_id
- Secrets audit: nenhum segredo hardcoded, .env*.local no gitignore
- Headers: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, HSTS, Permissions-Policy já configurados
- 31 testes passando (3 novos para validação de input)

### File List
| File | Action | Description |
|------|--------|-------------|
| `bellus/src/app/actions/equipe.ts` | Modified | Email validation, role enum, nome/telefone length, cross-tenant service check |
| `bellus/src/app/actions/servicos.ts` | Modified | Added salao_id to updateServico, cross-tenant profissional check, preco_override validation |
| `bellus/src/app/actions/agendamentos.ts` | Modified | Added salao_id to cancelAgendamento cascade and rescheduleAgendamento |
| `bellus/src/app/actions/public-booking.ts` | Modified | Validate servico and profissional belong to salão |
| `bellus/src/app/api/booking/slots/route.ts` | Modified | Added rate limiting (30 req/min per IP) |
| `bellus/src/app/actions/__tests__/equipe.test.ts` | Modified | Added 3 tests for email, role, nome length validation |
