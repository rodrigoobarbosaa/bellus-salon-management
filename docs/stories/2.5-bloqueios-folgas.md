# Story 2.5 — Bloqueio de Horários e Folgas

**Epic:** EPIC-02 Agenda & Serviços
**Status:** Done
**Prioridade:** MUST HAVE
**Depende de:** Story 2.2 (calendário)
**PRD Refs:** FR-01.3

---

## Story

Como **proprietário**, quero **bloquear horários e registrar folgas** para profissionais, para que o sistema não permita agendamentos nesses períodos e a agenda reflita a disponibilidade real.

---

## Acceptance Criteria

- [x] AC-01: Proprietário pode criar bloqueio para um profissional (data/hora início e fim)
- [x] AC-02: Proprietário pode criar folga de dia inteiro para um profissional
- [x] AC-03: Bloqueios aparecem no calendário com visual distinto (cor cinza, hachurado)
- [x] AC-04: Sistema impede criar agendamento em horário bloqueado
- [x] AC-05: Proprietário pode remover bloqueio existente
- [x] AC-06: Campo de motivo opcional (ex: "Almuerzo", "Vacaciones", "Curso")
- [x] AC-07: Profissional pode ver seus próprios bloqueios no calendário

---

## Dev Notes

### Nova tabela: `bloqueios`
Precisa de migration para criar:
```sql
CREATE TABLE bloqueios (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  profissional_id UUID NOT NULL REFERENCES profissionais(id) ON DELETE CASCADE,
  salao_id UUID NOT NULL REFERENCES saloes(id) ON DELETE CASCADE,
  data_hora_inicio TIMESTAMPTZ NOT NULL,
  data_hora_fim TIMESTAMPTZ NOT NULL,
  dia_inteiro BOOLEAN NOT NULL DEFAULT false,
  motivo TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT chk_bloqueio_horario CHECK (data_hora_fim > data_hora_inicio)
);
```

### RLS
Mesmo padrão multi-tenant: `salao_id = public.get_salao_id()`

### Integração com conflito
A query de conflito do Story 2.3 deve incluir bloqueios:
```sql
-- Verificar bloqueio
SELECT id FROM bloqueios
WHERE profissional_id = $1
  AND data_hora_inicio < $new_fim
  AND data_hora_fim > $new_inicio
```

### Padrões
- Migration nova para tabela `bloqueios`
- Atualizar `Database` type em `types/supabase.ts`
- Server Actions: `createBloqueio`, `deleteBloqueio`
- Bloqueios renderizados como background events no FullCalendar

---

## Tasks

- [x] Task 1: Criar migration para tabela `bloqueios` com RLS
- [x] Task 2: Atualizar tipos TypeScript (`Database` type)
- [x] Task 3: Criar Server Actions: `createBloqueio`, `deleteBloqueio`
- [x] Task 4: Criar modal de criação de bloqueio (horário ou dia inteiro)
- [x] Task 5: Renderizar bloqueios no calendário como background events
- [x] Task 6: Integrar validação de bloqueio na criação de agendamento (Story 2.3)
- [x] Task 7: Implementar remoção de bloqueio (click + confirmação)
- [x] Task 8: Aplicar migration no Supabase remoto (SQL Editor)
- [x] Task 9: Validação e testes manuais

---

## File List

| Arquivo | Ação |
|---------|------|
| `src/app/(dashboard)/dashboard/agenda/bloqueio-form.tsx` | Existente — Modal criar bloqueio (horário + dia inteiro + motivo) |
| `src/app/actions/bloqueios.ts` | Existente — Server Actions createBloqueio, deleteBloqueio |
| `src/app/(dashboard)/dashboard/agenda/agenda-view.tsx` | Existente — Background events + click detail + delete |
| `src/app/actions/agendamentos.ts` | Existente — Validação de bloqueio no createAgendamento |

---

## Dev Agent Record

### Debug Log
- Implementação já existia completa no codebase (pré-implementada)
- Tabela `bloqueios` já criada com RLS multi-tenant
- BloqueioForm: toggle dia inteiro/horário, profissional select, motivo
- Bloqueios renderizados como `display: "background"` com cores cinza
- createAgendamento já verifica conflito com bloqueios
- Click em bloqueio abre modal com info + botão "Eliminar bloqueo"

### Completion Notes
- Todos os 7 ACs atendidos: criar bloqueio, dia inteiro, visual calendário, impedimento agendamento, remover, motivo, visualização profissional

### Change Log

| Data | Alteração |
|------|-----------|
| 2026-03-01 | Story criada |
| 2026-03-15 | Story verificada e marcada como Done por @dev (Dex) |

---
*@sm (River) — 2026-03-01*
