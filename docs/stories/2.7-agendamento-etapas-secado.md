# Story 2.7 — Agendamento com Etapas (Coloração + Secado)

**Epic:** EPIC-02 Agenda & Serviços
**Status:** Done
**Prioridade:** MUST HAVE
**Depende de:** Story 2.1 (serviços), Story 2.3 (criar agendamento)
**PRD Refs:** —

---

## Story

Como **profissional**, quero **agendar serviços com etapas separadas** (ex: coloração + secado), para que o tempo de pausa/processamento fique livre na minha agenda e eu possa atender outra cliente nesse intervalo.

---

## Contexto de Negócio

Serviços como coloração de raiz, mechas e tratamentos químicos possuem um **tempo de processamento** entre a aplicação e a finalização (secado/enxágue). Durante esse tempo, a profissional está **livre** para atender outra cliente.

**Exemplo real:**
```
10:00 — Aplicação de coloração de raiz (20 min) → profissional OCUPADA
10:20 → 11:00 — Tempo de processamento (40 min) → profissional LIVRE
11:00 — Secado/enxágue/escova (25 min) → profissional OCUPADA
```

A profissional precisa poder:
1. Agendar a coloração normalmente
2. **Opcionalmente** adicionar o secado como etapa separada
3. Escolher o horário do secado (não necessariamente logo após a pausa)
4. Ter a agenda livre durante o processamento

---

## Acceptance Criteria

### Schema & Configuração
- [x] AC-01: Tabela `servicos` possui campos opcionais `tempo_pausa_minutos` e `duracao_pos_pausa_minutos` para serviços com etapas
- [x] AC-02: Ao criar/editar serviço de categoria `coloracao`, `mechas` ou `tratamento`, exibir campos de configuração de pausa e secado
- [x] AC-03: Estes campos são opcionais — serviços simples (corte) não são afetados

### Agendamento com Etapas
- [x] AC-04: Ao criar agendamento para serviço com pausa configurada, exibir opção "Adicionar secado"
- [x] AC-05: Se ativado, formulário mostra campo de horário para o secado com sugestão automática (início + duração + pausa)
- [x] AC-06: Profissional pode ajustar o horário do secado livremente (não obrigatoriamente logo após a pausa)
- [x] AC-07: Secado é criado como agendamento vinculado ao agendamento principal (via `agendamento_pai_id`)
- [x] AC-08: Cada etapa tem `tipo_etapa`: `aplicacao` (primeira parte) ou `secado` (segunda parte); agendamentos normais ficam como `unico`
- [x] AC-09: Validação de conflito aplica-se a cada etapa individualmente (secado não pode sobrepor outro agendamento — respeitando regra de conflito da Story 2.3)

### Visualização no Calendário
- [x] AC-10: Ambas etapas aparecem no calendário com vínculo visual (mesma cor, ícone de link, ou linha conectora)
- [x] AC-11: O gap entre etapas aparece como tempo livre na agenda do profissional
- [x] AC-12: Ao clicar em qualquer etapa, exibe detalhes do agendamento completo (aplicação + secado)

### Edição & Cancelamento
- [x] AC-13: Cancelar agendamento principal cancela também o secado vinculado
- [x] AC-14: Secado pode ser reagendado independentemente (apenas horário, não serviço)
- [ ] AC-15: Se agendamento principal muda de horário, sistema sugere novo horário para secado — **DEFERRED: requer UI adicional no edit mode**

---

## Dev Notes

### Alterações no Schema

**Tabela `servicos` — novos campos:**
```sql
ALTER TABLE servicos ADD COLUMN tempo_pausa_minutos INT CHECK (tempo_pausa_minutos > 0);
ALTER TABLE servicos ADD COLUMN duracao_pos_pausa_minutos INT CHECK (duracao_pos_pausa_minutos > 0);
COMMENT ON COLUMN servicos.tempo_pausa_minutos IS 'Tempo de processamento/pausa entre etapas (ex: 40 min para cor processar)';
COMMENT ON COLUMN servicos.duracao_pos_pausa_minutos IS 'Duração da etapa final/secado após a pausa';
```

**Tabela `agendamentos` — novos campos:**
```sql
CREATE TYPE tipo_etapa_agendamento AS ENUM ('unico', 'aplicacao', 'secado');
ALTER TABLE agendamentos ADD COLUMN agendamento_pai_id UUID REFERENCES agendamentos(id) ON DELETE CASCADE;
ALTER TABLE agendamentos ADD COLUMN tipo_etapa tipo_etapa_agendamento NOT NULL DEFAULT 'unico';
COMMENT ON COLUMN agendamentos.agendamento_pai_id IS 'Referência ao agendamento principal (para secado vinculado)';
COMMENT ON COLUMN agendamentos.tipo_etapa IS 'unico=normal, aplicacao=primeira etapa, secado=etapa final pós-pausa';
```

### Cálculo de sugestão automática
```
horario_sugerido_secado = data_hora_inicio + duracao_minutos + tempo_pausa_minutos
duracao_secado = duracao_pos_pausa_minutos
```

### Padrões
- Reutilizar componente `AgendamentoForm` com seção expansível para secado
- Server Action `createAgendamento` deve suportar criação atômica (aplicação + secado em transaction)
- Calendário: agendamentos com `agendamento_pai_id` renderizados com vínculo visual

---

## Tasks

- [x] Task 1: Criar migration para novos campos em `servicos` e `agendamentos`
- [x] Task 2: Atualizar tipos TypeScript (`Database` type)
- [x] Task 3: Atualizar formulário de serviço (Story 2.1) com campos de pausa e secado
- [x] Task 4: Implementar seção "Adicionar secado" no formulário de agendamento
- [x] Task 5: Implementar sugestão automática de horário do secado
- [x] Task 6: Atualizar Server Action `createAgendamento` para criação atômica de etapas
- [x] Task 7: Atualizar validação de conflito para considerar etapas individuais
- [x] Task 8: Implementar visualização vinculada no calendário
- [x] Task 9: Implementar cancelamento em cascata e reagendamento de secado
- [x] Task 10: Validação e testes manuais

---

## File List

| Arquivo | Ação |
|---------|------|
| `supabase/migrations/20260315000001_etapas_secado.sql` | Criado — ALTER TABLE servicos + agendamentos, CREATE TYPE tipo_etapa_agendamento |
| `src/app/actions/servicos.ts` | Modificado — createServico/updateServico incluem tempo_pausa_minutos e duracao_pos_pausa_minutos |
| `src/app/(dashboard)/dashboard/servicos/servico-form.tsx` | Modificado — Campos de pausa/secado para categorias coloracao/mechas/tratamento |
| `src/app/(dashboard)/dashboard/servicos/servicos-list.tsx` | Modificado — Interface Servico atualizada com novos campos |
| `src/app/(dashboard)/dashboard/servicos/page.tsx` | Modificado — Type cast atualizado com novos campos |
| `src/app/(dashboard)/dashboard/agenda/agendamento-form.tsx` | Modificado — Seção "Añadir secado" com sugestão automática |
| `src/app/actions/agendamentos.ts` | Modificado — createAgendamento cria aplicação+secado atômico, cancelamento em cascata |
| `src/app/(dashboard)/dashboard/agenda/agenda-view.tsx` | Modificado — Ícones etapa no calendário (🎨 aplicação, 💨 secado), tipos atualizados |
| `src/app/(dashboard)/dashboard/agenda/page.tsx` | Modificado — Fetch inclui campos pausa/secado |

---

## Dev Agent Record

### Debug Log
- Migration criada: ALTER TABLE servicos (2 campos), ALTER TABLE agendamentos (agendamento_pai_id + tipo_etapa), CREATE TYPE + CREATE INDEX
- ServicoForm: campos de pausa/secado aparecem condicionalmente quando categoria é coloracao/mechas/tratamento
- AgendamentoForm: checkbox "Añadir secado" aparece quando serviço tem pausa configurada, auto-sugestão de horário
- createAgendamento: criação atômica (INSERT principal + INSERT secado), tipo_etapa=aplicacao/secado/unico
- cancelAgendamento: cascata busca filhos com agendamento_pai_id e cancela todos
- Calendário: prefixo emoji 🎨/💨 para diferenciar etapas visualmente
- TypeScript compila limpo após atualizar todas as interfaces

### Completion Notes
- 14 de 15 ACs atendidos. AC-15 (sugestão automática de novo horário ao mover principal) diferido
- Feature completa: config serviço → agendar com secado → visualização → cancelamento cascata
- Migration precisa ser aplicada no Supabase remoto

### Change Log

| Data | Alteração |
|------|-----------|
| 2026-03-15 | Story criada por Orion (@aios-master) a pedido do usuário |
| 2026-03-15 | Implementação completa por @dev (Dex). 14/15 ACs, AC-15 deferred |

---
*@aios-master (Orion) — 2026-03-15*
